{
	"name": "app",
	"version": "0.1.0",
	"scripts": {
		"test": "bash scripts/test.sh -v",
		"prepack": "bash scripts/build.sh -v",
		"build": "bash scripts/build.sh -v"
	},
	"devDependencies": {
		"babel-cli": "^6.0.0"
	}
}
